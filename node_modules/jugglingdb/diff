diff --git a/lib/validatable.js b/lib/validatable.js
index b6dc5d6..4c30be8 100644
--- a/lib/validatable.js
+++ b/lib/validatable.js
@@ -255,9 +255,10 @@ function validateUniqueness(attr, conf, err, done) {
     var cond = {where: {}};
     cond.where[attr] = this[attr];
     this.constructor.all(cond, function (error, found) {
+        console.log(found);
         if (found.length > 1) {
             err();
-        } else if (found.length === 1 && found[0].id != this.id) {
+        } else if (found.length === 1 && (!this.id || !found[0].id || found[0].id.toString() != this.id.toString())) {
             err();
         }
         done();
